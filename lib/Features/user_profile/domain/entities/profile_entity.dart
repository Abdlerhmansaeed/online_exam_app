class ProfileEntity {
  String? userName;
  String? email;
  String ? phone;
  String ? firstName;
  String ? lastName;
  String ? id;

  ProfileEntity({this.userName, this.email, this.phone, this.firstName, this.lastName, this.id});
}